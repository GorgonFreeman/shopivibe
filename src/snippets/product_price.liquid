{%- if product -%}
  <product-price>
    {%- if product.price_varies -%}
      <span class="product-price__from">{{ 'products.price_from' | t }}</span>
    {%- endif -%}
    {%- if product.compare_at_price > product.price -%}
      <s class="product-price__compare">{{ product.compare_at_price | money }}</s>
    {%- endif -%}
    <span class="product-price__current{% if product.compare_at_price > product.price %} sale{% endif %}">{{ product.price | money }}{%- if product.compare_at_price > product.price %} ({{ product.compare_at_price | minus: product.price | times: 100 | divided_by: product.compare_at_price | round }}% off){%- endif -%}</span>
  </product-price>
{%- endif -%}
