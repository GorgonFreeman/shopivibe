<script>
  (async () => {
    // TODO: Error handling
    await window.Shopify.loadFeatures([
      { 
        name: 'consent-tracking-api', 
        version: '0.1', 
        onLoad: () => {
          document.dispatchEvent(new CustomEvent('shopify:features:consent-tracking-api:ready'));
        },
      },
    ]);
  })();
</script>