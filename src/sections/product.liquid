{{ product | json }}

<div class="product-form">
  {% render 'variant_picker', product: product %}
  {% render 'buy_button', product: product %}
</div>

{% schema %}
  {
    "name": "Product",
    "settings": []
  }
{% endschema %}
