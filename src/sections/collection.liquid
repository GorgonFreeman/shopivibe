{% paginate collection.products by 10 %}
  <collection-products>
    {% for product in collection.products %}
      {% render 'product_tile', handle: product.handle %}
    {% endfor %}
    <button data-ref="loadMoreButton">load more</button>
  </collection-products>

  {{ paginate | default_pagination }}
{% endpaginate %}

{% schema %}
  {
    "name": "Collection",
    "settings": []
  }
{% endschema %}
