{% if section.settings.source == 'collection' and section.settings.collection != blank %}
  {% assign products = section.settings.collection.products %}
{% elsif section.settings.source == 'products' %}
  {% assign products = section.settings.products %}
{% endif %}

{% if section.settings.source == 'collection' and section.settings.limit > 0 %}
  {% assign limit = section.settings.limit %}
{% endif %}

{% for product in products limit: limit %}
  {% render 'product_tile', product: product %}
{% endfor %}

{% schema %}
  {
    "name": "Product tiles",
    "settings": [
      {
        "type": "select",
        "id": "source",
        "label": "Source",
        "options": [
          {
            "value": "collection",
            "label": "Collection"
          },
          {
            "value": "products",
            "label": "Products"
          }
        ],
        "default": "collection"
      },
      {
        "type": "product_list",
        "id": "products",
        "label": "Products"
      },
      {
        "type": "collection",
        "id": "collection",
        "label": "Collection"
      },
      {
        "type": "number",
        "id": "limit",
        "label": "Limit",
        "default": 12,
        "visible_if": "{{ section.settings.source == 'collection' }}"
      }
    ],
    "presets": [
      {
        "name": "Product tiles"
      }
    ]
  }
{% endschema %}
