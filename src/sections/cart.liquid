{% comment %}{{ cart | json }}{% endcomment %}

{% if cart.item_count > 0 %}
  {% render 'cart_items' %}
{% else %}
  {% render 'cart_empty' %}
{% endif %}

{% schema %}
  {
    "name": "Cart",
    "settings": []
  }
{% endschema %}
