<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    {% render 'vite', entry: 'src/styles/theme.css' %}
    {{ content_for_header }}
  </head>
  <body>
    hihihi
    {{ content_for_layout }}
  </body>
</html>
