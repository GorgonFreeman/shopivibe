<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    {{ content_for_header }}
    <link rel="stylesheet" href="{{ 'main.css' | asset_url }}">
  </head>
  <body>
    {% assign __script_keys = '' %}
    {{ content_for_layout }}
    {% render 'js_translations' %}
    <script src="{{ 'main.js' | asset_url }}" defer></script>
  </body>
</html>
